name: asterix
description: A Asterix decode library.
version: 1.0.0
repository: https://github.com/izaque93/asterix_dec

environment:
  sdk: ^3.8.0-171.0.dev

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
